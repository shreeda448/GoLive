services:
  db:
    image: postgres:alpine
    environment:
      - POSTGRES_PASSWORD=iuytrewa
      - POSTGRES_DB=go_live
      - POSTGRES_USER=shreeda
    ports:
      - "5433:5432"
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=admin
      - MINIO_ROOT_PASSWORD=password@212
    command: server /data --console-address ":9001"
